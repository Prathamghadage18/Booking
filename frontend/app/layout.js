export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>Restaurant Table Booking</title>
      </head>
      <body>{children}</body>
    </html>
  );
}
